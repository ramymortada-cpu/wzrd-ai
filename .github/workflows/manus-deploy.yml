###############################################################################
# Legacy placeholder — full CI/CD lives in ci.yml (WZRD monorepo).
# Kept as manual-only so we do not duplicate PR/push runs with the same name.
###############################################################################
name: Manus Deploy (manual)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use CI/CD workflow (.github/workflows/ci.yml) for automated checks."
